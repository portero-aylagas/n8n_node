# n8n Node Reference Table

| Node | Workflow | Parameters | Settings | What It Does | JSON Input | JSON Output | Key Transformations |
|---|---|---|---|---|---|---|---|
| When chat message received | AI Agent | `public: true`; `initialMessages: "Hello tell me a joke about n8n"`; `options: {}` | Chat trigger node; public chat enabled | Starts the workflow when a user sends a message in the n8n chat UI | User chat message from chat interface | Item with `action`, `sessionId`, and `chatInput` | Converts chat UI input into structured n8n JSON |
| AI Agent | AI Agent | `options: {}` | Connected to Anthropic Chat Model through `ai_languageModel`; connected to Simple Memory through `ai_memory`; no tool connected | Receives chat input, sends it to the model, uses memory context, and returns the assistant answer | `action`, `sessionId`, `chatInput` | `output` string containing generated answer | Converts user request into generated AI response |
| Anthropic Chat Model | AI Agent | Model `claude-opus-4-5-20251101`; cached result name `Claude Opus 4.5`; `options: {}` | Uses Anthropic credential reference; secret value excluded | Provides the LLM used by the AI Agent | Prompt/model request from AI Agent | Model completion returned to AI Agent | Converts agent prompt into natural-language text |
| Simple Memory | AI Agent | No exported parameters | Connected through `ai_memory` | Stores recent conversation context for the same session | Session/chat context | Memory context provided to agent | Adds session-based context; does not directly change visible final JSON |
| Schedule Trigger | Example Class | Rule interval: weekly | Trigger node; execution order `v1` at workflow level | Starts the news workflow on a schedule | No incoming node data | Timestamp object with readable date/time fields | Generates time metadata used as workflow input |
| When clicking 'Execute workflow' | Example Class | No parameters | Manual trigger node | Starts the workflow manually from the editor | Manual click | Empty/manual trigger item | Allows testing without waiting for the schedule |
| Message a model | Example Class | Model `gpt-5.4-mini`; OpenAI Responses operation; built-in web search enabled; allowed domains list; JSON schema output format `news_analysis`; `simplify: false` | Uses OpenAI credential reference; text format is strict JSON schema; web search context size low | Searches curated AI news sources and generates structured AI-news analysis | Timestamp item from Schedule Trigger or Manual Trigger | Full OpenAI response object with `web_search_call` entries and final structured message | Turns date/time input into structured AI news analysis JSON |
| Format Blocks | Example Class | JavaScript code in Code node | Code node type version 2 | Parses the OpenAI response, extracts structured story data, and formats it as Notion block objects | OpenAI response object with `output`, message content, stories, and summary fields | One item with `blocks` array and `citations` array | Converts model JSON into Notion-compatible `heading_1`, `heading_2`, and `paragraph` blocks; chunks long text for Notion's rich-text limits |
| Split Out | Example Class | `fieldToSplitOut: citations` | Default options | Splits the `citations` array into separate items | One item containing `blocks` and `citations` array | One item per citation | Converts an array field into multiple n8n items |
| Create a page | Example Class | Parent page URL; title expression `AI News Analysis - $now`; first block from `$json.blocks[0]`; `simple: false` | Uses Notion credential reference | Creates a Notion page under the selected parent page | `blocks` array and `citations` array | Notion page object with page id, title, parent, timestamps, and URL | Converts workflow JSON into a new Notion page resource |
| HTTP Request | Example Class | Method `PATCH`; URL expression `https://api.notion.com/v1/blocks/{{ $('Create a page').item.json.id }}/children`; predefined Notion credential; headers `Notion-Version: 2022-06-28`, `Content-Type: application/json`; JSON body with children blocks | Uses Notion API credential reference | Appends the generated block list to the created Notion page | Notion page object from Create a page; references blocks from Format Blocks | Notion API page/block response | Sends formatted blocks to Notion API as page children |
| Append row in sheet | Example Class | Operation `append`; document and sheet fields present but empty in export | Node is present and disabled in exported workflow | Would append citation rows to a sheet if enabled and configured | Split citation items from Split Out | Not executed in the captured successful run | Intended path: citation items to tabular rows |
